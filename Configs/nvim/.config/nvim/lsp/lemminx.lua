return {
  filetypes = { "xml", "xsd", "xslt", "svg" },
  init_options = {
    settings = {
      xml = {
        format = {
          enabled = true,
          joinContentLines = true,
          preservedNewlines = 1,
          insertSpaces = true,
          tabSize = 4,
        },
      },
    },
  },
}
